server:
  port: 7777

spring:
  application:
    name: API-
  boot:
    admin:
      client:
        url: http://localhost:8888/
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/



